* Must pass the current ENV to the server when adding a new command (flag to switch this off).
* Show ENV in verbose listing.

* ringio -k <session-name> -> forbicly remove a session socket.

* Use a decent (non-std library) flags parsing library.

* Reimplement as library with different backends:
  - On linux, implement a epoll backend
  - Backend without ringbuffer

* Add documentation to all public functions of the new package.

* seekable ringbuf (reader can seek to beginning or end.)
